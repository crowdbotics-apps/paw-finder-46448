import React from "react";
import { View, StyleSheet, SafeAreaView } from "react-native";

const GetStarted = () => {
  return <SafeAreaView style={styles.safeArea}>
        
    <View style={styles.rFePASkD}></View></SafeAreaView>;
};

const styles = StyleSheet.create({
  safeArea: {
    height: '100%',
    backgroundColor: "#682fb1"
  },
  rFePASkD: {
    height: 700,
    width: 400,
    backgroundColor: "#a661c5",
    borderRadius: 0,
    color: "#777777"
  }
});
export default GetStarted;